{
  "rules": {
    "status": {
      "$userId": {
        ".read": "$userId === auth.uid",
        ".write": "$userId === auth.uid"
      }
    },
    ".read": "auth != null",
    ".write": "auth != null"
  }
}
